<STARTDOC>
macro(vindex())
(Generate an index entry for LaTex() or texinfo v-indices. 
Its argument is the index entry. 
See also the tt([cfkpt]index) macro.)
<>

DEFINEMACRO(vindex)(1)(\
<latex>
    NOTRANS(\index{)\
    ARG1\
    +CHAR(})\
<texinfo>
    NOTRANS(@vindex )\
    ARG1 \
<>
)
