<STARTDOC>
macro(tt(text))
    (Sets tt(text) in teletype font, and prevents it from being expanded.
For unbalanced parameter lists, use tt(CHAR(C)HAR(40)) to get
tt(CHAR(40)) and tt(CHAR(C)HAR(41)) to get tt(CHAR(41)).

    The tt(tt) macro does interpret character tables as well as any
tt(SUBST) definitions. This is usually what is intended. In situations where
tt(tt) performs unwelcome character table or tt(SUBST) transformations, it is
usually possible to specify multiple tt(tt) sequences. E.g., to prevent the
character substitution of tt(\")tt(e) from being used write tt(tt(\")tt(e)).

 In html the tt(attrib) macro applies to the tt(<code>) tag.
 )
<>

DEFINEMACRO(tt)(1)(\
<html>
    XXtag(code)\
    NOEXPAND(ARG1)\
    NOTRANS(</code>)\
<latex>
    NOTRANS(\texttt{)\
    NOEXPAND(ARG1)\
    CHAR(})\
<texinfo>
    NOTRANS(@code{)\
    NOEXPAND(ARG1)\
    CHAR(})\
<man ms>
    NOTRANS(\f)\
    CHAR(40)CW\
    +NOEXPAND(ARG1)\
    NOTRANS(\fP)\
<sgml>
    NOTRANS(<tt>)\
    NOEXPAND(ARG1)\
    NOTRANS(</tt>)\
<xml>
    NOTRANS(<tt>)\
    NOEXPAND(ARG1)\
    NOTRANS(</tt>)\
<else>
    NOEXPAND(ARG1)\
<>
)

