mailto(karel@icce.rug.nl)
latexoptions(dina4)

COMMENT(
	urg, html breaks...
        (PIPETHROUGH(echo -n `date '+%d/%b/%y'|tr '[a-z]' '[A-Z]'`)())
)
manpage(striproff)
       (1)
       (@DATE@)
       (The Yodl Package)
       (Yodl)

manpagename(striproff)(strip ASCII control characters)

manpagesynopsis()

    em(command |) bf(striproff) em(> outputfile) nl()
    
manpagedescription()

    The bf(striproff) program is a very simple tool to strip ASCII control
    characters off a stream, so that only `bare' ASCII is left. The stripped
    control sequences are ^H (backspace) followed by any character; this
    mechanism is used to underline or boldface text on terminals that support
    it. 

    bf(striproff) is typically used with the Yodl converters bf(yodl2msless)
    and bf(yodl2manless). 

manpageoptions()

manpagefiles()

manpageseealso()
gagmacrowarning(yodlconverters)

    bf(yodlconverters(1)): information on bf(yodl2msless) and
    bf(yodl2manless).

manpagediagnostics()

manpagebugs()

    Send bug reports and fixes to the Yodl list: email(yodl@icce.rug.nl)

manpageauthor()
Please consult the documentation file file(AUTHORS.txt) for more detailed
information, and small contributions. 

itemize(
it()nemail(Karel Kubat)(karel@icce.rug.nl)
    lurl(http://www.icce.rug.nl/karel/karel.html)
it()nemail(Jan Nieuwenhuizen)(janneke@gnu.org)
    lurl(http://www.xs4all.nl/~jantien)
)
