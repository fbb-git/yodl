tt(SYMBOLVALUE) expands to the value of a symbol. Its single argument
must be the name of a symbol.  The symbol must have been created earlier
using tt(DEFINESYMBOL).
    nl()   
    Example:
        verb(
    The symbol has value SYMBOLVALUE(MYSYMBOL).
        )
