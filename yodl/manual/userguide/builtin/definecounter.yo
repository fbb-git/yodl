tt(DEFINECOUNTER) creates a new counter. This builtin function expects two
parameter list: the name of the counter and its initial value.
    
    Examples:
        verb(
    DEFINECOUNTER(YEAR)(1950)
    DEFINECOUNTER(NTIMES)(0)
        )
    See also section ref(COUNTERS) and the tt(USECOUNTER) builtin function.
