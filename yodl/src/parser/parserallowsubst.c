#include "parser.ih"

void parser_allow_subst(register Parser *pp, bool onOff)
{
//    lexer_setSubst(&pp->d_lexer, 
//                   onOff == true ? pp->d_subst : pp->d_emptySubst);

//    subst_allow(&pp->d_subst, onOff);
}
