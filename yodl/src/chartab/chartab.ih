#include "chartab.h"

#include <stdio.h>
#include <ctype.h>
#include <string.h>

#include "../message/message.h"
#include "../new/new.h"

size_t      ct_ascii(char const *ch, size_t *increment);
char      **ct_construct(char *table);
void        ct_destroy(void *);
void        ct_parse_table(char **chartab, char *table);
char       *ct_string(char const *str);

